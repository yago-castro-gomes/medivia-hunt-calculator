import React, { useState } from 'react'

export default function Calculatorfunc() {
  const [ uh, setUh ] = useState(0);
  const [ hmm, setHmm ] = useState(0);
  const [ sd, setSd ] = useState(0);
  const [ gfb, setGfb ] = useState(0);
  const [ explo, setExplo ] = useState(0);
  const [ explosive, setExplosive ] = useState(0);
  const [ charparticipate1, setCharparticipate1 ] = false;
  const [ uhwaste, setUhWaste ] = useState(0);

  return (
    <div>Calculatorfunc</div>
  )
}
